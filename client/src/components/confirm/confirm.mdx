import { Meta, Story, Canvas, ArgTypes } from '@storybook/addon-docs';
import Confirm, { Primary } from './confirm.stories';

<Meta title='Components/Confirm' of={Confirm} />

# Confirm Component

Global component ，that provides secondary validation and retrieves results through callback.
When multiple simultaneous calls occur, the last content is shown first.

## Code Usage

```tsx
import { ConfirmContext } from '@client/components/confirm/context';

export const Example = () => {
    const { showConfirm } = useContext(ConfirmContext);

    const handleShowConfirm = () => {
        showConfirm('confirm message', (confirm) => {
            if (confirm) {
                console.log('confirmed');
            } else {
                console.log('canceled');
            }
        });
    };
};
```

## Example Usage

<Canvas>
    <Story of={Primary} />
</Canvas>
