import { Meta, Story, Canvas, ArgTypes } from '@storybook/addon-docs';
import Tabs, { Primary } from './tabs.stories';

<Meta title='Components/Tabs' of={Tabs} />

# Tabs Component

The `Tabs` component can quickly and easily implement a browser-like tab page feature, currently supporting only display and tab switching.

## Example Usage

<Canvas>
    <Story of={Primary} />
</Canvas>

## Props

<ArgTypes />
